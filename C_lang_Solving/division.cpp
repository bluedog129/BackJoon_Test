#include <stdio.h>

int main() {
	int baekM = 0;
	int divisionValue = 0;

	scanf("%d %d", &baekM, &divisionValue);

	if ((baekM >= divisionValue) && (baekM < 10^1000) && ) {
		printf("%d\n%d", baekM / divisionValue, baekM % divisionValue);
	}
}